import type { MiddlewareHandler } from 'hono';
import type { Hono } from 'hono/tiny';
import type { Unstable_ProcessRequest as ProcessRequest } from '../types.js';
export declare function rscMiddleware({ processRequest, }: {
    processRequest: ProcessRequest;
}): MiddlewareHandler;
export declare function middlewareRunner(middlewareModules: Record<string, () => Promise<{
    default: (opts: {
        app: Hono;
    }) => MiddlewareHandler;
}>>, opts: {
    app: Hono;
}): MiddlewareHandler;
