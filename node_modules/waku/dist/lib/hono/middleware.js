export function rscMiddleware({ processRequest }) {
    return async (c, next)=>{
        const req = c.req.raw;
        const res = await processRequest(req);
        if (res) {
            c.res = res;
            return;
        }
        await next();
    };
}
export function middlewareRunner(middlewareModules, opts) {
    let handlersPromise;
    return async (c, next)=>{
        if (!handlersPromise) {
            handlersPromise = Promise.all(Object.values(middlewareModules).map((m)=>m().then((mod)=>mod.default(opts))));
        }
        const handlers = await handlersPromise;
        let response;
        const run = async (index)=>{
            const handler = handlers[index];
            if (handler) {
                const result = await handler(c, ()=>run(index + 1));
                if (result && !response) {
                    response = result;
                }
            } else {
                await next();
            }
        };
        await run(0);
        return response;
    };
}

//# sourceMappingURL=middleware.js.map