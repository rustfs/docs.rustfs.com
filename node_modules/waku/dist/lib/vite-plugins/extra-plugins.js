import react from '@vitejs/plugin-react';
export function extraPlugins(config) {
    const plugins = [
        ...config.vite?.plugins ?? []
    ];
    // add react plugin automatically if users didn't include it on their own (e.g. swc, oxc, babel react compiler)
    if (!plugins.flat().some((p)=>p && 'name' in p && p.name.startsWith('vite:react'))) {
        plugins.push(react());
    }
    return plugins;
}

//# sourceMappingURL=extra-plugins.js.map