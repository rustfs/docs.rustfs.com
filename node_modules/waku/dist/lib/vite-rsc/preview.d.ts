import type { IncomingMessage, ServerResponse } from 'node:http';
type PreviewServerMiddlewares = {
    use: (fn: (req: IncomingMessage, res: ServerResponse, next: (err?: unknown) => void) => void) => void;
};
export type PreviewServer = {
    baseUrl: string;
    middlewares: PreviewServerMiddlewares;
    close: () => Promise<void>;
};
export declare function startPreviewServer(): Promise<PreviewServer>;
export {};
