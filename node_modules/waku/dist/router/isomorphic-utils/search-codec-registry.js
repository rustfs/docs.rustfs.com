export const isCodec = (value)=>typeof value === 'object' && value !== null && typeof value.id === 'string' && typeof value.parse === 'function' && typeof value.serialize === 'function';
/**
 * Resolve a route path to its search codec id, using the `route -> codec id`
 * map that define-router ships as `globalThis.__WAKU_ROUTER_SEARCH_CODECS__`.
 * Lets `push`/`Link` serialize `search` for any route, not just the current one.
 */ export const getRouteSearchCodecId = (routePath)=>{
    const map = globalThis.__WAKU_ROUTER_SEARCH_CODECS__;
    return map?.[routePath];
};

//# sourceMappingURL=search-codec-registry.js.map