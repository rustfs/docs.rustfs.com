export type BuildOptions = {
    srcDir: string;
    distDir: string;
    DIST_PUBLIC: string;
    serverless: boolean;
};
export default function buildEnhancer(build: (utils: unknown, options: BuildOptions) => Promise<void>): Promise<typeof build>;
