import { ImgHTMLAttributes, ReactNode } from "react";
//#region src/components/image.d.ts
interface StaticImageData {
  src: string;
  width: number;
  height: number;
}
interface ImageProps extends Omit<ImgHTMLAttributes<HTMLImageElement>, "src"> {
  src?: string | StaticImageData;
  /** a value between 1-100, the quality of optimized image */
  quality?: number;
  /** Act like a normal `<img>` tag, default to `false` except for SVG images */
  unoptimized?: boolean;
  /** Preload the image @default true */
  preload?: boolean;
}
declare function Image({ src: _src, width: _width, height: _height, sizes, quality, unoptimized, preload, ...rest }: ImageProps): import("react").JSX.Element;
//#endregion
export { Image, ImageProps, StaticImageData };